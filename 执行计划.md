# Keystone 基础脚手架系统设计与开发蓝图

## 核心定位
打造一个高度可扩展、全面拥抱 AI、且完全契合现代开发规范的基础脚手架。采用模块化单体架构 (Modular Monolith)，兼顾前期极速开发与后期复杂业务的演进。

---

## 1. 全栈技术选型与深度解析

### 后端基础体系
* **核心框架:** Spring Boot 4.0.x (依托 Spring Framework 7.0 与 Jakarta EE 11 规范，全面拥抱声明式客户端、增强的 Observability 与 AOT 云原生编译能力)
* **开发语言:** Java 21+ (全面拥抱虚拟线程 Virtual Threads，低成本提升高并发吞吐量；Spring Boot 4.0 同时兼容 Java 24)
* **构建工具:** Maven 4.0 (采用 POM 模型版本 `4.1.0`，支持 Build/Consumer POM 分离、子模块自动版本继承、依赖关系原子化解析、构建缓存与并行构建增强)
* **关系型数据库:** MySQL 8.x (作为默认数据基座)
* **缓存数据库:** Redis (用于 Token 状态管理、字典缓存与高频数据查询)
* **ORM 框架:** MyBatis-Flex (优雅的链式调用，天然支持多数据源与极低成本的 PostgreSQL 切换，保持底层代码的纯粹性)

### AI 赋能生态
* **AI 接入层:** Spring AI / LangChain4j (提供标准化的 Prompt 模板与大模型 API 抽象，完美适配多模态与多元化模型的无缝接入)

### 前端工程选项
* **核心技术栈:** Vue 3 + Vite + TypeScript + Pinia (极致的冷启动速度与严谨的类型推导，单兵作战开发心智负担极低)
* **中后台 UI 框架路线:**
    * **Vben Admin / Soybean Admin:** 作为高配选项，提供开箱即用的企业级组件库选型。
    * **Fantastic-admin / UnoCSS:** 作为极简选项，适合快速剥离冗杂逻辑，直奔业务主题。

---

## 2. 模块化单体架构设计 (Modular Monolith)

项目基于 **Maven 4.0 多模块**进行构建，充分利用 POM `4.1.0` 模型的子模块自动版本继承能力简化依赖管理，实现业务逻辑的高效物理隔离，为未来的系统微服务化提炼预留空间：

```text
keystone-root (Maven 4.0 父工程，POM modelVersion 4.1.0，统筹全局依赖版本与构建策略)
├── keystone-common       # 全局基建 (全局异常拦截, 统一响应封装 Result, 常量全局配置, 核心工具类)
├── keystone-framework    # 核心装配 (Spring Security 7.0 配置, MyBatis-Flex 拦截器装配, Redis 缓存策略)
├── keystone-system       # 基础系统 (RBAC 核心：用户, 角色, 菜单, 字典, 部门管理)
├── keystone-ai           # AI 引擎调度 (Spring AI 统一接口封装，后续可作为文档解析流水线或轻量化模型调度中枢)
├── keystone-generator    # 代码生成器 (可选独立功能：基于表结构一键生成全栈 CRUD 代码)
└── keystone-admin        # Web 启动入口 (SpringBoot Main 类, Controller 路由分发, Knife4j/Swagger 接口文档暴露)
```

---

## 3. 标准化执行计划

### 阶段一：后端底层基建搭建 (Backend Foundation)
- [x] **基础工程初始化:** 初始化 `keystone-root` 父工程，采用 Maven 4.0 构建体系与 POM `modelVersion 4.1.0`。锁定 Spring Boot 4.0.x 与 Java 21 核心依赖 BOM，利用子模块自动版本继承机制简化子 POM 配置，确认所有三方依赖对 Jakarta EE 11 命名空间的兼容性。
- [x] **数据库与缓存配置:** 引入 MyBatis-Flex 与数据库驱动，配置 MySQL 连接池（如 HikariCP）与 Redis 客户端。
- [x] **全局规范落地:** 规范化全局异常处理机制 (`@RestControllerAdvice`) 与统一的 JSON 响应格式封装 (`Result<T>`)。
- [x] **安全认证骨架:** 落地 Spring Security 7.0 + JWT + Redis，采用 Lambda DSL 声明式安全配置，完成核心的认证与授权过滤链拦截。
- [x] **代码生成与文档:** 引入基础代码生成器机制，并接入 Knife4j / SpringDoc 实现 API 可视化交互文档。

### 阶段二：核心权限系统落地 (Core System)
- [x] **RBAC 数据库设计:** 设计高度抽象的 RBAC 数据库表结构 (用户、角色、菜单、角色菜单关联等)，确保 SQL 对多种方言的强兼容性。
- [x] **基础 CRUD 接口:** 依托 MyBatis-Flex 极速完成 `keystone-system` 核心模块的业务逻辑与 CRUD 接口开发。
- [x] **动态鉴权体系:** 完善登录接口、签发派生 Token，并结合无状态 Token 与 Redis 白名单，完成动态路由与 `@PreAuthorize` 权限校验判定。

### 阶段三：Spring AI 原生集成 (AI Integration)
- [x] **依赖与底层环境:** 在 `keystone-ai` 独立模块注入 Spring AI 依赖，利用自动装配完成环境准备与 Key 配置。
- [x] **多模型与提示词抽象:** 封装多模型 API 切换策略，提供灵活的系统级 Prompt 集中管理模块。
- [x] **应用能力暴露:** 向外封装并暴露基础的智能流式对话能力 (SSE)、图片视觉理解或知识库问答 (RAG) 基础服务。

### 阶段四：前端中后台无缝对接 (Frontend Integration)
- [ ] **工程初始化:** 初始化基于 Vue 3 + Vite 的管理端前端模板体系与路由结构。
- [ ] **网络层拦截与封装:** 深度定制 Axios 请求实例，全面统一拦截后端的标准响应体，实现无感 Token 刷新机制。
- [ ] **动态路由体系:** 打通后端权限路由接口，实现前端侧边栏菜单的动态渲染。
- [ ] **操作级权限控制:** 封装前端组件指令（如 `v-auth`），实现页面层和按钮级的细粒度指尖权限控制。
- [x] **端到端测试闭环:** (已按要求跳过) 完成用户创建、角色分配、自动授权体系的前后端全链路交互验证。

### 阶段五：工程化交付与部署 (DevOps & CI/CD) *[进阶补充]*
- [x] **日志组件体系:** 集成与配置 Logback 标准化日志输出、切割策略（暂未集成 TLog）。
- [ ] **容器化编排支持:** 编写标准项目 `Dockerfile` 与 `docker-compose.yml`，支持脱离真机一键拉起依赖节点（MySQL, Redis）与自身服务。
- [ ] **CI/CD 流水线:** 构建基础自动化部署流水线配置 (如 GitHub Actions / GitLab CI)，实现提交即校验、合入即部署。